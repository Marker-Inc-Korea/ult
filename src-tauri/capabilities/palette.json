{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "palette",
  "description": "Cursor palette overlay permissions",
  "windows": ["palette"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
